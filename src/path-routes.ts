const app=document.querySelector<HTMLDivElement>('#app');
const labels=['Find a Skill','Find Work','Start a Business','Side Income','Freelance','Not Sure - Show Me Options'];
function shell(title:string,body:string){if(!app)return;app.innerHTML=`<main class="shell"><header><p class="eyebrow">Find My Path</p><h1>${title}</h1><p>${body}</p></header><article class="card"><p><strong>GUIDANCE:</strong> This route is being built as a guided discovery path. It will not invent jobs, income, customers or opportunities that have not been supported by evidence.</p></article><button class="mic" type="button" aria-label="Voice input"><span aria-hidden="true">●</span> Any Indian language</button><nav class="nav" aria-label="Navigation"><button id="path-route-back" type="button">← Back</button><button id="path-route-home" type="button">Home</button></nav></main>`;document.querySelector('#path-route-back')?.addEventListener('click',()=>history.back());document.querySelector('#path-route-home')?.addEventListener('click',()=>{location.href='/';});}
document.addEventListener('click',(event)=>{const target=event.target as HTMLElement|null;const button=target?.closest<HTMLButtonElement>('[data-p]');if(!button)return;const i=Number(button.dataset.p);if(i===2)return;const title=labels[i];if(!title)return;queueMicrotask(()=>shell(title,i===5?'We will use your broad preferences to show relevant paths while keeping every choice optional.':'Explore this route without creating a marketplace, transaction system, or unsupported promise.'));});
