const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
const read=()=>{try{return JSON.parse(localStorage.getItem('skill-aur-dhandha-home-tiffin-simulator')||'{}') as Record<string,string>}catch{return {}}};
const n=(s:Record<string,string>,k:string)=>Math.max(0,Number(s[k])||0);
function summary(){const s=read(),price=n(s,'price'),meals=n(s,'meals'),days=n(s,'days'),variable=n(s,'ingredients')+n(s,'packaging')+n(s,'delivery'),fixed=n(s,'fixed'),sales=price*meals*days,total=variable*meals*days+fixed,surplus=sales-total,margin=sales?surplus/sales*100:0;return{price,meals,days,sales,total,surplus,margin}}
function download(name:string,type:string,text:string){const url=URL.createObjectURL(new Blob([text],{type})),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function exportReport(){const r=summary();download('skill-aur-dhandha-home-tiffin-report.html','text/html;charset=utf-8',`<!doctype html><meta charset="utf-8"><title>Home Tiffin Report</title><h1>Skill Aur Dhandha — Home Tiffin Report</h1><p><strong>YOUR CALCULATION — scenario only, not an income guarantee.</strong></p><h2>Scenario</h2><p>${money(r.price)} per meal · ${r.meals} meals/day · ${r.days} days/month</p><h2>Calculation summary</h2><p>Sales: ${money(r.sales)}<br>Total costs: ${money(r.total)}<br>Modeled surplus: ${money(r.surplus)}<br>Margin: ${r.margin.toFixed(1)}%</p><h2>Evidence boundary</h2><p>Official sector context does not establish demand or profitability for an individual home-tiffin business.</p><h2>Reality check</h2><p>Historical performance is not a guarantee. Validate local demand and pilot before committing substantial money.</p><p>Use Print / Save as PDF to create a PDF copy.</p>`)}
function exportSheet(){const r=summary(),rows=[['Metric','Value'],['Selling price per meal',r.price],['Meals per day',r.meals],['Operating days per month',r.days],['Monthly sales',r.sales],['Monthly total costs',r.total],['Modeled monthly surplus',r.surplus],['Scenario margin %',r.margin.toFixed(1)],['Boundary','Scenario calculation only; not a forecast or income guarantee.']];download('skill-aur-dhandha-home-tiffin-calculations.csv','text/csv;charset=utf-8',rows.map(row=>row.map(v=>`"${String(v).replace(/"/g,'""')}"`).join(',')).join('\n'))}
function showReport(){const app=document.querySelector<HTMLDivElement>('#app');if(!app)return;const r=summary();app.innerHTML=`<main class="shell"><header><p class="eyebrow">Home Tiffin</p><h1>Premium Report</h1><p>PREVIEW — review what the paid report would contain before any purchase.</p></header><article class="card"><h2>Report preview</h2><p><strong>1. Your scenario:</strong> ${money(r.price)} per meal · ${r.meals} meals/day · ${r.days} days/month.</p><p><strong>2. Calculation summary:</strong> sales ${money(r.sales)}, total costs ${money(r.total)}, modeled surplus ${money(r.surplus)}, margin ${r.margin.toFixed(1)}%.</p><p><strong>3. Evidence:</strong> official sector context, source, reference period, verified date and limitations.</p><p><strong>4. Business health:</strong> 13-point rule-based review with evidence and assumption gaps.</p><p><strong>5. Scenarios:</strong> baseline, What-if comparison and Reality Check.</p><p><strong>6. Next actions:</strong> learning, approvals route, market-validation guidance and pilot-first guidance.</p></article><article class="card"><h2>Purchase / unlock</h2><p><strong>TEST MODE:</strong> no payment provider is connected. Test unlock charges ₹0 and creates no purchase.</p><button class="choice" id="test-unlock" type="button">Test unlock report<span>›</span></button></article><article class="card" id="exports"><h2>Report + spreadsheet export</h2><p>Locked until test unlock. Production payment remains a separate release gate.</p></article><button class="mic" type="button" aria-label="Voice input"><span aria-hidden="true">●</span> Any Indian language</button><nav class="nav" aria-label="Navigation"><button id="report-back" type="button">← Back</button><button id="report-home" type="button">Home</button></nav></main>`;document.querySelector('#test-unlock')?.addEventListener('click',()=>{const out=document.querySelector<HTMLElement>('#exports');if(!out)return;out.innerHTML='<h2>Test unlocked</h2><p><strong>₹0 charged · no purchase created.</strong></p><div class="actions"><button class="choice" id="export-report" type="button">Create printable report<span>›</span></button><button class="choice" id="export-sheet" type="button">Download spreadsheet data<span>›</span></button></div><p>Printable report can be saved as PDF using the browser print command. Spreadsheet data is CSV compatible with Excel.</p>';document.querySelector('#export-report')?.addEventListener('click',exportReport);document.querySelector('#export-sheet')?.addEventListener('click',exportSheet)});document.querySelector('#report-back')?.addEventListener('click',()=>location.reload());document.querySelector('#report-home')?.addEventListener('click',()=>{localStorage.setItem('skill-aur-dhandha-s1c-home','1');location.reload()})}
document.addEventListener('click',(e)=>{const t=e.target as HTMLElement|null;if(t?.closest('#report')){e.preventDefault();e.stopImmediatePropagation();showReport()}},true);
