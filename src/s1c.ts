const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
const read=()=>JSON.parse(localStorage.getItem('skill-aur-dhandha-home-tiffin-simulator')||'{}') as Record<string,string>;
const n=(s:Record<string,string>,k:string)=>Math.max(0,Number(s[k])||0);
function summary(){const s=read(),price=n(s,'price'),meals=n(s,'meals'),days=n(s,'days'),variable=n(s,'ingredients')+n(s,'packaging')+n(s,'delivery'),fixed=n(s,'fixed'),sales=price*meals*days,total=variable*meals*days+fixed,surplus=sales-total,margin=sales?surplus/sales*100:0;return{price,meals,days,sales,total,surplus,margin}}
function showReport(){const app=document.querySelector<HTMLDivElement>('#app');if(!app)return;const r=summary();app.innerHTML=`<main class="shell"><header><p class="eyebrow">Home Tiffin</p><h1>Premium Report</h1><p>PREVIEW — review what the paid report would contain before any purchase.</p></header><article class="card"><h2>Report preview</h2><p><strong>1. Your scenario:</strong> ${money(r.price)} per meal · ${r.meals} meals/day · ${r.days} days/month.</p><p><strong>2. Calculation summary:</strong> sales ${money(r.sales)}, total costs ${money(r.total)}, modeled surplus ${money(r.surplus)}, margin ${r.margin.toFixed(1)}%.</p><p><strong>3. Evidence:</strong> official sector context, source, reference period, verified date and limitations.</p><p><strong>4. Business health:</strong> the 13-point rule-based review with evidence and assumption gaps.</p><p><strong>5. Scenarios:</strong> baseline, What-if comparison and Reality Check.</p><p><strong>6. Next actions:</strong> learning, approvals route, market-validation guidance and pilot-first guidance.</p></article><article class="card"><h2>Purchase / unlock</h2><p><strong>TEST MODE:</strong> no payment provider is connected in this Factory X proof. Tapping the test unlock charges ₹0 and does not create a purchase.</p><button class="choice" id="test-unlock" type="button">Test unlock report<span>›</span></button></article><article class="card" id="exports"><h2>PDF + Excel</h2><p>Locked in this proof until the test unlock is used. Production purchase and file-export plumbing must pass the later payment, privacy and release gates.</p></article><button class="mic" type="button" aria-label="Voice input"><span aria-hidden="true">●</span> Any Indian language</button><nav class="nav" aria-label="Navigation"><button id="report-back" type="button">← Back</button><button id="report-home" type="button">Home</button></nav></main>`;document.querySelector('#test-unlock')?.addEventListener('click',()=>{const out=document.querySelector<HTMLElement>('#exports');if(out)out.innerHTML='<h2>Test unlocked</h2><p><strong>₹0 charged · no purchase created.</strong></p><p>PDF report: export target confirmed for the production stage.</p><p>Excel workbook: export target confirmed for the production stage.</p><p>This proof does not claim that production payment or downloadable PDF/Excel files are implemented yet.</p>'});document.querySelector('#report-back')?.addEventListener('click',()=>location.reload());document.querySelector('#report-home')?.addEventListener('click',()=>{localStorage.setItem('skill-aur-dhandha-s1c-home','1');location.reload()})}
document.addEventListener('click',(e)=>{const t=e.target as HTMLElement|null;if(t?.closest('#report')){e.preventDefault();e.stopImmediatePropagation();showReport()}},true);
