import './style.css';

type Screen = 'home' | 'path' | 'filters' | 'categories' | 'possibilities' | 'evidence' | 'simulator';
const appRoot = document.querySelector<HTMLDivElement>('#app');
if (!appRoot) throw new Error('App root not found');
const app: HTMLDivElement = appRoot;
const homeButtons = ['Find My Path', 'Find My Market', 'Paisa Check', 'Learn & Grow', 'Government Help'];
const pathButtons = ['Find a Skill', 'Find Work', 'Start a Business', 'Side Income', 'Freelance', 'Not Sure - Show Me Options'];
const businessCategories = ['Food', 'Retail', 'Service / Skill', 'Manufacturing', 'Agriculture / Farm Business', 'Digital / Online', 'Home-based'];
let screen: Screen = 'home';
function go(next: Screen) { screen = next; render(); }
function mic() { return `<button class="mic" type="button" aria-label="Voice input"><span aria-hidden="true">●</span> Any Indian language</button>`; }
function nav(back: Screen, showNext = false) { return `<nav class="nav" aria-label="Navigation"><button id="back" type="button">← Back</button><button id="home" type="button">Home</button>${showNext ? '<button id="next" type="button">Next →</button>' : ''}</nav>`; }
function wireNav(back: Screen, next?: Screen) { app.querySelector<HTMLButtonElement>('#back')?.addEventListener('click', () => go(back)); app.querySelector<HTMLButtonElement>('#home')?.addEventListener('click', () => go('home')); if (next) app.querySelector<HTMLButtonElement>('#next')?.addEventListener('click', () => go(next)); }
function render() {
  if (screen === 'home') { app.innerHTML = `<main class="shell"><header><p class="eyebrow">India Opportunity Navigator</p><h1>Skill Aur Dhandha</h1><p>Discover realistic paths. Understand the numbers. Choose your next step.</p></header><section class="actions">${homeButtons.map((label,i)=>`<button class="choice" data-index="${i}">${label}<span>›</span></button>`).join('')}</section>${mic()}</main>`; app.querySelector<HTMLButtonElement>('.choice[data-index="0"]')?.addEventListener('click',()=>go('path')); return; }
  if (screen === 'path') { app.innerHTML = `<main class="shell"><header><p class="eyebrow">Find My Path</p><h1>What are you looking for?</h1></header><section class="actions">${pathButtons.map((label,i)=>`<button class="choice" data-path-index="${i}">${label}<span>›</span></button>`).join('')}</section>${mic()}${nav('home')}</main>`; app.querySelector<HTMLButtonElement>('.choice[data-path-index="2"]')?.addEventListener('click',()=>go('filters')); wireNav('home'); return; }
  if (screen === 'filters') { const saved=JSON.parse(localStorage.getItem('skill-aur-dhandha-filters')||'{}') as Record<string,string>; app.innerHTML=`<main class="shell"><header><p class="eyebrow">Start a Business</p><h1>Optional reality filters</h1><p>These are optional. You can continue without selecting anything.</p></header><form class="card" id="filters"><label>Investment band<input name="investment" value="${saved.investment||''}" placeholder="Optional" /></label><label>Time availability<input name="time" value="${saved.time||''}" placeholder="Optional" /></label><label>Operating location<input name="location" value="${saved.location||''}" placeholder="Optional" /></label><label>Broad geography<input name="geography" value="${saved.geography||''}" placeholder="Optional" /></label></form>${mic()}${nav('path',true)}</main>`; const save=()=>{const form=app.querySelector<HTMLFormElement>('#filters');if(form)localStorage.setItem('skill-aur-dhandha-filters',JSON.stringify(Object.fromEntries(new FormData(form))));}; app.querySelector<HTMLButtonElement>('#back')?.addEventListener('click',()=>{save();go('path')}); app.querySelector<HTMLButtonElement>('#home')?.addEventListener('click',()=>{save();go('home')}); app.querySelector<HTMLButtonElement>('#next')?.addEventListener('click',()=>{save();go('categories')}); return; }
  if (screen === 'categories') { app.innerHTML=`<main class="shell"><header><p class="eyebrow">Start a Business</p><h1>Choose a category</h1></header><section class="actions">${businessCategories.map(label=>`<button class="choice category" type="button" data-category="${label}">${label}<span>›</span></button>`).join('')}</section>${mic()}${nav('filters')}</main>`; app.querySelectorAll<HTMLButtonElement>('.category').forEach(button=>button.addEventListener('click',()=>{localStorage.setItem('skill-aur-dhandha-selected-category',button.dataset.category||'');go('possibilities')})); wireNav('filters'); return; }
  if (screen === 'evidence') { app.innerHTML=`<main class="shell"><header><p class="eyebrow">Home Tiffin</p><h1>Evidence Snapshot</h1><p>VERIFIED FACT — sector context, not a guarantee of demand or profit for an individual tiffin business.</p></header><article class="card"><h2>Registered food-processing sector GVA</h2><p>₹91,667.66 crore (2014-15) → ₹187,712.60 crore (2022-23).</p><p>Official series also reports ₹103,878.31 crore (2015-16), ₹123,397.26 crore (2016-17), ₹130,647.39 crore (2017-18), ₹135,743.13 crore (2018-19), ₹137,572.58 crore (2019-20), ₹151,698.70 crore (2020-21), and ₹163,204.40 crore (2021-22).</p><p><strong>Source:</strong> Ministry of Food Processing Industries, Annual Report 2024-25. <strong>Reference:</strong> 2014-15 to 2022-23. <strong>Verified:</strong> 17 Sep 2026.</p><p><strong>Limitation:</strong> This is registered food-processing sector evidence; it does not measure home-tiffin demand, revenue or profitability.</p></article><article class="card"><h2>Food-business regulatory context</h2><p>FSSAI states that food business operators are required to be licensed or registered. Its 2026 amendment FAQ states revised turnover thresholds apply from 1 Apr 2026.</p><p><strong>Source:</strong> FSSAI / FoSCoS. <strong>Verified:</strong> 17 Sep 2026.</p></article><div class="actions"><a class="choice" href="https://www.mofpi.gov.in/documents/reports/annual-report" target="_blank" rel="noopener noreferrer">View MoFPI source<span>›</span></a><a class="choice" href="https://foscos.fssai.gov.in/" target="_blank" rel="noopener noreferrer">View FSSAI / FoSCoS<span>›</span></a></div>${mic()}${nav('possibilities')}</main>`; wireNav('possibilities'); return; }
  if (screen === 'simulator') {
    const defaults: Record<string,string> = { price:'100', meals:'25', days:'26', ingredients:'40', packaging:'8', delivery:'0', fixed:'5000' };
    const saved = JSON.parse(localStorage.getItem('skill-aur-dhandha-home-tiffin-simulator') || '{}') as Record<string,string>;
    const value = (key:string) => saved[key] || defaults[key];
    app.innerHTML=`<main class="shell"><header><p class="eyebrow">Home Tiffin</p><h1>Business Simulator</h1><p>SCENARIO — editable starting assumptions, not verified market prices or an income promise.</p></header><form class="card" id="simulator"><label>Selling price per meal (₹)<input type="number" min="0" step="1" name="price" value="${value('price')}" /></label><label>Meals sold per day<input type="number" min="0" step="1" name="meals" value="${value('meals')}" /></label><label>Operating days per month<input type="number" min="0" max="31" step="1" name="days" value="${value('days')}" /></label><label>Ingredients per meal (₹)<input type="number" min="0" step="1" name="ingredients" value="${value('ingredients')}" /></label><label>Packaging per meal (₹)<input type="number" min="0" step="1" name="packaging" value="${value('packaging')}" /></label><label>Delivery/platform cost per meal (₹)<input type="number" min="0" step="1" name="delivery" value="${value('delivery')}" /></label><label>Other monthly fixed costs (₹)<input type="number" min="0" step="1" name="fixed" value="${value('fixed')}" /></label><p><strong>Your calculation:</strong> Change every assumption to match your own situation before relying on the result.</p></form>${mic()}${nav('possibilities',true)}</main>`;
    const save=()=>{const form=app.querySelector<HTMLFormElement>('#simulator');if(form)localStorage.setItem('skill-aur-dhandha-home-tiffin-simulator',JSON.stringify(Object.fromEntries(new FormData(form))));};
    app.querySelector<HTMLButtonElement>('#back')?.addEventListener('click',()=>{save();go('possibilities')});
    app.querySelector<HTMLButtonElement>('#home')?.addEventListener('click',()=>{save();go('home')});
    app.querySelector<HTMLButtonElement>('#next')?.addEventListener('click',()=>{save();const form=app.querySelector<HTMLFormElement>('#simulator');if(form){const data=Object.fromEntries(new FormData(form));const status=document.createElement('p');status.id='simulator-status';status.setAttribute('aria-live','polite');status.textContent='Inputs saved. Basic Result is the next build stage.';form.appendChild(status);}}); return;
  }
  const selectedCategory=localStorage.getItem('skill-aur-dhandha-selected-category')||'Selected category'; const foodCard=selectedCategory==='Food'?`<article class="card"><p class="eyebrow">Food</p><h2>Home Tiffin</h2><p>A home-based meal-service possibility. Review evidence before judging demand, costs or income potential.</p><div class="actions"><button class="choice" id="view-evidence" type="button">View Evidence<span>›</span></button><button class="choice" id="test-business" type="button">Test Business<span>›</span></button></div><p id="s13-status" aria-live="polite"></p></article>`:`<article class="card"><h2>${selectedCategory}</h2><p>Possibility content for this category has not yet passed the evidence gate.</p></article>`; app.innerHTML=`<main class="shell"><header><p class="eyebrow">${selectedCategory}</p><h1>Business possibilities</h1></header>${foodCard}${mic()}${nav('categories')}</main>`; app.querySelector<HTMLButtonElement>('#view-evidence')?.addEventListener('click',()=>{localStorage.setItem('skill-aur-dhandha-selected-business','food_home_tiffin');go('evidence')}); app.querySelector<HTMLButtonElement>('#test-business')?.addEventListener('click',()=>{localStorage.setItem('skill-aur-dhandha-selected-business','food_home_tiffin');go('simulator')}); wireNav('categories');
}
render();
