const relevant=()=>{const t=document.querySelector('#app main')?.textContent?.toLowerCase()||'';return t.includes('farm produce')||t.includes('farmer')||t.includes('agriculture')||t.includes('find my market');};
function inject(){const main=document.querySelector<HTMLElement>('#app main');if(!main||!relevant()||main.querySelector('[data-export-market-bridge]'))return;const card=document.createElement('article');card.className='card';card.dataset.exportMarketBridge='true';card.innerHTML=`<p class="eyebrow">VALUE-ADDED / EXPORT</p><h2>Explore export-ready product ideas</h2><p>Moringa powder, dry banana powder, spice powders, beetroot powder and biomass briquettes are retained as value-added opportunities.</p><button class="choice" id="open-export-market-bridge" type="button">Explore export opportunities<span>›</span></button><p><strong>Affiliate readiness:</strong> relevant retail-ready products, equipment, packaging, testing and logistics tools can support future affiliate monetisation only after an authorised commercial programme and permitted link are verified. No Instamart or other retailer relationship is implied today.</p>`;const nav=main.querySelector('nav.nav');nav?main.insertBefore(card,nav):main.append(card);}
document.addEventListener('click',e=>{const t=e.target as HTMLElement;if(t.closest('#open-export-market-bridge'))document.dispatchEvent(new Event('open-export-affiliate-opportunities'));});
const observer=new MutationObserver(()=>queueMicrotask(inject));observer.observe(document.documentElement,{subtree:true,childList:true});queueMicrotask(inject);
